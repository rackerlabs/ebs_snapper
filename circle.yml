machine:
  python:
    version: '2.7.10'

dependencies:
  pre:
    - pip install -U pip
    - pip install -U tox tox-pyenv
    - pyenv local 2.7.10

test:
  override:
    - aws cloudformation validate-template --region us-east-1 --template-body=file://cloudformation.json
    - tox
